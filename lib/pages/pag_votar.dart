import 'package:flutter/material.dart';

class PaginaVotar extends StatelessWidget {
  const PaginaVotar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("Votar", style: TextStyle(fontSize: 30),),
    );
  }
}